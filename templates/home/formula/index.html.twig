{% extends 'base.html.twig' %}

{% block title %}Quai Antique|La carte{% endblock %}

{% block body %}

<h1 class="text-center m-5">Nos formules</h1>
    <div>
        {% for formulaitem in formula %}
            <div class="card m-5">
                <div class="card-body">
                    <h5 class="card-title">{{ formulaitem.title | raw }}</h5>
                    <p class="card-text">{{ formulaitem.description | raw }}</p>
                </div>
                <div class="card-footer">{{ formulaitem.price | raw }}€</div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
