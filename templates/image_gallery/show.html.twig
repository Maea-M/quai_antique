{% extends 'base.html.twig' %}

{% block title %}Gallerie d'images{% endblock %}

{% block body %}
    <h1>Gallerie d'images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_gallery.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ image_gallery.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ image_gallery.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_gallery_index') }}">Retourner à la liste d'images</a>

    <a href="{{ path('app_image_gallery_edit', {'id': image_gallery.id}) }}">Modifier</a>

    {{ include('image_gallery/_delete_form.html.twig') }}
{% endblock %}
